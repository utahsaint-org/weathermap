#Weathermap v3 (SVG Edition)
#Created by:  Utah Education Network
#2014

This directory contains the JSON files that define the maps.

The default map must be called:     config.json

When you add new maps, you will prepend the name of the map to the config file.

An example would be:   home.config.json
